# version 0.5.12

## features

- `is_municipality()` is no longer an internal function


## enhancements

- `get_extent()` with `buffer = ...` now buffers the bounding box in EPSG:3034 instead of EPSG:25832


## bug fixes 

- 
